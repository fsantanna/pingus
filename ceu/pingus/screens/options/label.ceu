#ifdef _LABEL_CEU
#error multiple include
#else
#define _LABEL_CEU

code/await Label (var _char_const_ptr label, var& Rect rect) -> FOREVER
do
    every SDL_REDRAW do
        call {Fonts::chalk_normal}.render(
            _origin_top_left,
            rect.left,
            rect.top,
            label,
            *{Display::s_framebuffer}
        );
    end
end

#endif
